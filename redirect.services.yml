services:
  redirect_subscriber:
    class: Drupal\redirect\EventSubscriber\RedirectSubscriber
    arguments: ['@url_generator', '@entity.manager', '@language_manager']
    tags:
      - { name: event_subscriber }
